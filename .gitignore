target/
notes.md
.vscode
node_modules
dist/
pkg/

# perf stuff
flamegraph.svg
perf.data*
profile.json
/*.json
/*.txt

_scratch.egg
/*.egg
*.log
*.dot
*.svg
*.DS_Store
# macOS AppleDouble sidecar files (the Herbie dump programs originated on macOS)
._*

# racket
scripts/compiled
tests/*.json

lcov.info

# eval: extracted benchmark corpora (e.g. Herbie dumps)
*.extracted/

# eval: stray harness temp benchmark files (the bench runner writes
# `tmp<random>.egg` next to a benchmark and unlinks on clean exit; a killed
# run can leave them, and they must never be committed into the corpus)
tmp*.egg

# Python virtualenv (local eval/eval-live tooling)
venv/
/.perf-wt/

# Agent worktrees + session tooling
.claude/

# Scratch / experiment workspace
.tmp/

# Python bytecode caches
__pycache__/
*.pyc

# eval scratch result dumps
eval/herbie_sample_results.json
eval/resultsjun19.json
eval/results.json
